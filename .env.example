# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000

# Feature Flags
NEXT_PUBLIC_DEBUG_MODE=false

# Mock API: tỉ lệ giả lập lỗi server (0 = tắt, 1 = luôn lỗi).
# Dùng để demo error state / rollback. VD: MOCK_FAIL_RATE=0.3 -> ~30% request lỗi 500.
MOCK_FAIL_RATE=0
